class Note {
  String noteID;
  String noteTitle;
  String noteContent;
  DateTime createDateTime;
  DateTime lastestEditDateTime;

  Note({this.noteID, this.noteContent, this.createDateTime, this.noteTitle,
      this.lastestEditDateTime});

}